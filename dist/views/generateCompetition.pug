html 
    head 
        title OAuth2/OpenID login demo
        style 
            include ../../public/style.css
        script 
            include ../../public/validate.js
    body
        h1 Generate competition
        ul
            li: a(href='/', title='Home') Home 
            li: a(href='/competition', title='Competition') Competition  

        form(action="/generateCompetition" method="POST" name="form")
            p Name of your competition:
            input(type="text" name="name" id="name" placeholder="e.g. competition1" required )

            p Competitors(seperate them using ,), there has to be min 4 and max 8 competitors:
            input(type="text" name="competitors" id="competitors" placeholder="e.g. name1,name2,name3,name4" required)

            p Type of competition:
            select(name='competitionType' id="competitionType" required)
                option(value='1') win/draw/lose, e.g. 3/0/1
                option(value='2') win/lose, e.g.: 2/1
            br
            br
            br
            input(type="submit" onclick="return validate()")